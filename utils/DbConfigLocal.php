<?php
function getCredentials():array{
    
    $credentials["username"]='root';
    $credentials['password']='';
    $credentials['server']='localhost:3308';
    $credentials['database']='webshop';
    
    return $credentials;
}
